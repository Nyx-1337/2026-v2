<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Chào mừng — Trang chính</title>
  <meta name="description" content="Chào mừng đến với trang của chúng tôi — nơi chia sẻ thông tin, sự kiện và kết nối với cộng đồng." />

  <meta property="og:title" content="Chào mừng — Trang chính" />
  <meta property="og:description" content="Chào mừng đến với trang của chúng tôi — nơi chia sẻ thông tin, sự kiện và kết nối với cộng đồng." />
  <meta property="og:type" content="website" />

  <link rel="icon" href="/favicon.ico" />

  <style>
    :root{
      --bg:#0f1724;
      --card:#0b1220;
      --accent:#06b6d4;
      --muted:#9aa4b2;
      --text:#e6eef6;
      --radius:12px;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      background: linear-gradient(180deg,var(--bg),#071025 80%);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
    }

    .container{
      max-width:1100px;
      margin:0 auto;
      padding:40px 20px;
    }

    header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:16px;
      margin-bottom:32px;
    }

    .logo{
      display:flex;
      align-items:center;
      gap:12px;
      text-decoration:none;
      color:var(--text);
    }
    .logo .mark{
      width:44px;
      height:44px;
      background:linear-gradient(135deg,var(--accent),#7c3aed);
      border-radius:10px;
      display:inline-block;
    }

    nav a{
      color:var(--muted);
      text-decoration:none;
      margin-left:18px;
      font-weight:600;
    }
    nav a.cta{
      color:var(--bg);
      background:var(--accent);
      padding:8px 14px;
      border-radius:10px;
      text-decoration:none;
      margin-left:18px;
    }

    .hero{
      display:grid;
      grid-template-columns:1fr;
      gap:24px;
      align-items:center;
    }
    @media(min-width:820px){
      .hero{grid-template-columns:1fr 420px}
    }

    .card{
      background:linear-gradient(180deg,rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:var(--radius);
      padding:28px;
      box-shadow: 0 6px 20px rgba(2,6,23,0.6);
    }

    h1{
      margin:0 0 12px 0;
      font-size:clamp(28px,4.5vw,44px);
      line-height:1.05;
      letter-spacing:-0.02em;
    }
    p.lead{color:var(--muted);margin:0 0 20px 0;font-size:17px}

    .cta-row{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }
    .btn{
      display:inline-block;
      padding:12px 18px;
      border-radius:10px;
      text-decoration:none;
      font-weight:700;
      cursor:pointer;
    }
    .btn.primary{background:var(--accent);color:var(--bg)}
    .btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--text)}


    .features{
      display:grid;
      grid-template-columns:repeat(1,1fr);
      gap:12px;
      margin-top:18px;
    }
    @media(min-width:640px){
      .features{grid-template-columns:repeat(3,1fr)}
    }
    .feature{
      background:rgba(255,255,255,0.02);
      padding:14px;
      border-radius:10px;
      text-align:left;
    }
    .feature h4{margin:0 0 8px 0}
    .feature p{margin:0;color:var(--muted);font-size:14px}

  
    .subscribe h3{margin:0 0 8px 0}
    .subscribe p{margin:0 0 12px 0;color:var(--muted)}

    form input[type="email"]{
      width:100%;
      padding:12px;
      border-radius:10px;
      border:1px solid rgba(255,255,255,0.04);
      background:transparent;
      color:var(--text);
      margin-bottom:10px;
    }
    form .row{display:flex;gap:8px}
    form .row input{flex:1}
    form button{padding:10px 14px;border-radius:10px;border:0;background:var(--accent);color:var(--bg);font-weight:700;cursor:pointer}

    footer{
      margin-top:40px;
      text-align:center;
      color:var(--muted);
      font-size:14px;
    }

    .visitor{
      display:inline-block;
      margin-top:10px;
      background:rgba(255,255,255,0.03);
      padding:6px 10px;
      border-radius:999px;
      font-weight:600;
      color:var(--muted);
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a class="logo" href="#">
        <span class="mark" aria-hidden="true"></span>
        <div>
          <div style="font-weight:800">Tên Trang</div>
          <div style="font-size:12px;color:var(--muted);margin-top:2px">Mô tả ngắn, slogan</div>
        </div>
      </a>

      <nav>
        <a href="#about">Giới thiệu</a>
        <a href="#features">Tính năng</a>
        <a href="#contact">Liên hệ</a>
        <a class="cta" href="#subscribe">Tham gia</a>
      </nav>
    </header>

    <main class="hero">
      <section class="card">
        <h1>Chào mừng! Đây là nơi bạn kết nối và khám phá.</h1>
        <p class="lead">Trang này được tạo nhanh để bạn bắt đầu — thay đổi nội dung, màu sắc, và liên kết theo ý muốn. Mời mọi người truy cập và khám phá!</p>

        <div class="cta-row">
          <a class="btn primary" href="#subscribe">Tham gia ngay</a>
          <a class="btn ghost" href="#about">Tìm hiểu thêm</a>
        </div>

        <div id="features" style="margin-top:18px">
          <div class="features">
            <div class="feature">
              <h4>Thông tin & Sự kiện</h4>
              <p>Chia sẻ bài viết, thông báo sự kiện và tin nổi bật.</p>
            </div>
            <div class="feature">
              <h4>Cộng đồng</h4>
              <p>Kết nối người dùng, nhận phản hồi và tạo mối quan hệ.</p>
            </div>
            <div class="feature">
              <h4>Dễ triển khai</h4>
              <p>Deploy nhanh trên GitHub Pages, Vercel hoặc Netlify.</p>
            </div>
          </div>
        </div>
      </section>

      <aside class="card subscribe" id="subscribe" aria-labelledby="subscribeHeading">
        <h3 id="subscribeHeading">Đăng ký nhận thông tin</h3>
        <p>Nhập email để nhận thông báo về nội dung mới nhất.</p>

        <form id="subscribeForm" onsubmit="return false;">
          <input id="email" type="email" placeholder="Email của bạn" required />
          <div class="row">
            <input type="text" id="name" placeholder="Tên (không bắt buộc)" />
            <button id="sendBtn" type="submit">Đăng ký</button>
          </div>
          <div id="msg" style="margin-top:10px;color:var(--muted);font-size:14px"></div>
        </form>

        <div style="margin-top:14px;color:var(--muted);font-size:13px">
          Hoặc liên hệ trực tiếp:
          <div style="margin-top:8px">
            <a href="mailto:you@example.com" style="color:var(--text);text-decoration:none">you@example.com</a>
          </div>
        </div>

        <div id="visitor" class="visitor" aria-live="polite">Lượt truy cập: 0</div>
      </aside>
    </main>

    <section id="about" style="margin-top:28px" class="card">
      <h2>Giới thiệu</h2>
      <p style="color:var(--muted)">Viết ở đây nội dung giới thiệu về mục đích trang, đội ngũ, hoặc điều bạn muốn truyền tải đến khách truy cập. Cá nhân hóa phần này để tăng tính tương tác.</p>
    </section>

    <section id="contact" style="margin-top:18px" class="card">
      <h2>Liên hệ</h2>
      <p style="color:var(--muted)">Bạn có thể thêm form liên hệ, chat, hoặc liên kết mạng xã hội tại đây.</p>
    </section>

    <footer>
      © <span id="year"></span> Tên Trang — tất cả quyền được bảo lưu.
    </footer>
  </div>

  <script>
    // Year update
    document.getElementById('year').textContent = new Date().getFullYear();

    // Simple subscribe form handler (demo)
    const form = document.getElementById('subscribeForm');
    const emailInput = document.getElementById('email');
    const nameInput = document.getElementById('name');
    const msg = document.getElementById('msg');

    document.getElementById('sendBtn').addEventListener('click', () => {
      const email = emailInput.value.trim();
      if (!email || !/^\S+@\S+\.\S+$/.test(email)) {
        msg.textContent = 'Vui lòng nhập email hợp lệ.';
        return;
      }

      msg.textContent = 'Cảm ơn! Email của bạn đã được ghi nhận (demo).';
      emailInput.value = '';
      nameInput.value = '';
      setTimeout(()=>{ msg.textContent = ''; }, 5000);
    });


    const key = 'site_demo_visits_v1';
    let visits = Number(localStorage.getItem(key) || 0);
    visits++;
    localStorage.setItem(key, visits);
    document.getElementById('visitor').textContent = 'Lượt truy cập: ' + visits;


    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener('click', function(e){
        const target = document.querySelector(this.getAttribute('href'));
        if(target){
          e.preventDefault();
          target.scrollIntoView({behavior:'smooth', block:'start'});
        }
      });
    });
  </script>
</body>
</html>
